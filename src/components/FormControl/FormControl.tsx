import { styled } from "stitches.config";

export const FormControl = styled("div", {
  display: "flex",
  marginBottom: "$4",
  flexDirection: "column",

  label: {
    display: "block",
    marginBottom: "$1",
  },
});
