import { styled } from "stitches.config";

export const Header = styled("header", {
  height: "56px",
  display: "flex",
  padding: "0 $5",
  alignItems: "center",
  backgroundColor: "$shade850",
});
